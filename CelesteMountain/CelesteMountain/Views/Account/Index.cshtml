@{
    ViewData["Title"] = "Account";
}
<div class="text-center">
    <h1 class="display-4">Account</h1>
    <p>Log in or sign up here.</p>
    <!--Sub-page Links-->
    <div class="navigation m-0">
        <a href="@Url.Action("Index", "Account")" class="btn btn-info mr-2">Log-in (Disabled)</a>
        <a href="@Url.Action("Registration", "Account")" class="btn btn-info mr-2">Register</a>
    </div>
</div>